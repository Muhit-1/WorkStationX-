<Window x:Class="WorkStationX.Views.ReminderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WorkStationX reminder"
        Width="420" SizeToContent="Height"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ShowInTaskbar="False" Topmost="True"
        WindowStartupLocation="Manual">

    <Border Background="{DynamicResource Brush.Panel}"
            BorderBrush="{DynamicResource Brush.Prime}" BorderThickness="1"
            Padding="20,18">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="6" BlurRadius="24" Opacity="0.6" Color="Black" />
        </Border.Effect>

        <DockPanel>
            <Grid DockPanel.Dock="Top" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Ellipse Grid.Column="0" Width="9" Height="9" VerticalAlignment="Center"
                         Margin="0,0,10,0" Fill="{DynamicResource Brush.Prime}" />
                <TextBlock Grid.Column="1" Text="REMINDER"
                           Style="{StaticResource Text.Engraved}" VerticalAlignment="Center" />
                <TextBlock Grid.Column="2" Text="{Binding Title}"
                           Style="{StaticResource Text.Mono}" VerticalAlignment="Center" />
            </Grid>

            <TextBlock DockPanel.Dock="Top" Text="{Binding Message}"
                       Style="{StaticResource Text.Body}"
                       Foreground="{DynamicResource Brush.TextBright}"
                       FontSize="15.5" TextWrapping="Wrap" Margin="0,0,0,18" />

            <Grid DockPanel.Dock="Bottom">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="SNOOZE 10 MIN" HorizontalAlignment="Left"
                        Style="{StaticResource Button.Tool}" Click="OnSnooze" />
                <Button Grid.Column="1" Content="DISMISS" Width="130"
                        Style="{StaticResource Button.Prime}" Padding="10,10"
                        Click="OnDismiss" IsDefault="True" />
            </Grid>
        </DockPanel>
    </Border>
</Window>
