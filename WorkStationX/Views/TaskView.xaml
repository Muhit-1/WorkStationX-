<UserControl x:Class="WorkStationX.Views.TaskView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel Margin="32" VerticalAlignment="Center" HorizontalAlignment="Center">
        <TextBlock Text="Tasks &amp; Time Bank"
                   FontSize="28" FontWeight="SemiBold" HorizontalAlignment="Center" />
        <TextBlock Text="Countdown timer with a signed Time Bank ledger."
                   Opacity="0.6" Margin="0,8,0,0" HorizontalAlignment="Center" />
        <TextBlock Text="Coming in v0.3"
                   Opacity="0.4" Margin="0,24,0,0" HorizontalAlignment="Center" />
    </StackPanel>
</UserControl>
