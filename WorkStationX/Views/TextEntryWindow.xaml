<Window x:Class="WorkStationX.Views.TextEntryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add text"
        Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        ResizeMode="NoResize"
        Background="{DynamicResource Brush.Shell}">

    <Border Background="{DynamicResource Brush.BayFace}"
            BorderBrush="{DynamicResource Brush.EdgeWindow}" BorderThickness="1"
            Padding="20,18">
        <StackPanel>
            <TextBlock Text="LABEL" Style="{StaticResource Text.EngravedSmall}" Margin="0,0,0,8" />
            <TextBox x:Name="Input" Style="{StaticResource Input.Text}" Tag="Type the label" />

            <Grid Margin="0,16,0,0">
                <Button Content="CANCEL" HorizontalAlignment="Left" Width="120"
                        Style="{StaticResource Button.Transport}" Padding="10,9"
                        Click="OnCancel" IsCancel="True" />
                <Button Content="ADD" HorizontalAlignment="Right" Width="140"
                        Style="{StaticResource Button.Prime}" Padding="10,9"
                        Click="OnOk" IsDefault="True" />
            </Grid>
        </StackPanel>
    </Border>
</Window>
